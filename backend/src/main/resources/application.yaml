spring:
  application:
    name: expensetracker
  datasource:
    url: jdbc:mysql://localhost:3306/expensetracker
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always